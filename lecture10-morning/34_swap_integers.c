#include <stdio.h>

// TODO: complete this.
void swap_ints() {
}

int main() {
  int x = 10;
  int y = 24;
  printf("Before running the function: x = %d, y = %d\n", x, y);
  // TODO: Complete this
  // swap_ints();
  printf("After running the function: x = %d, y = %d\n", x, y);
  return 0;
}
